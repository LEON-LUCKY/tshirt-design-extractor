# T恤图案提取工具 - 环境变量配置示例
# 复制此文件为 .env 并填入实际的配置值

# Remove.bg API密钥
# 获取方式：访问 https://www.remove.bg/api 注册账号并获取API密钥
# Requirements: 2.2
VUE_APP_REMOVE_BG_API_KEY=your_remove_bg_api_key_here

# Remove.bg API端点
# 开发环境：使用本地代理避免 CORS 问题
# 生产环境：直接调用 API（Netlify/Vercel 部署）
VUE_APP_REMOVE_BG_API_ENDPOINT=https://api.remove.bg/v1.0/removebg

# 可选：Cloudinary配置（如果使用Cloudinary代替Remove.bg）
# VUE_APP_CLOUDINARY_CLOUD_NAME=your_cloud_name
# VUE_APP_CLOUDINARY_UPLOAD_PRESET=your_upload_preset

# 应用配置
# 最大文件大小（字节）- 默认10MB
VUE_APP_MAX_FILE_SIZE=10485760

# 图像压缩阈值（像素）- 超过此宽度的图像将被压缩
VUE_APP_COMPRESSION_THRESHOLD=2000

# 压缩后的最大宽度（像素）
VUE_APP_MAX_COMPRESSED_WIDTH=1500

# API请求超时时间（毫秒）
VUE_APP_API_TIMEOUT=30000

# 是否启用图案提取（只保留图案，去掉T恤本身）
# true: 只提取图案部分
# false: 保留整件T恤（移除背景）
VUE_APP_ENABLE_PATTERN_EXTRACTION=true

# 图案提取边界填充（像素）
VUE_APP_PATTERN_PADDING=5

# 是否启用调试模式
VUE_APP_DEBUG=false

# 成功消息显示时长（毫秒）
VUE_APP_SUCCESS_MESSAGE_DURATION=3000
